{
	"version": 2,
	"builds": [
		{
			"src": "nuxt.config.js",
            "use": "@nuxt/vercel",
			"config": {
				"functions": {
					"prebuild": "npx prisma generate &&  pnpm build"
				}
			}
		}
	],
    "installCommand": "pnpm install"
}

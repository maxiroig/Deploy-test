{
	"version": 2,
	"builds": [
		{
            "config": {
                "functions": {
                    "prebuild": "npx prisma generate &&  pnpm build"
                }
            }
		}
	],
	"installCommand": "pnpm install --no-frozen-lockfile"
}

{
  "version": 2,
  "builds": [
    {
      "src": "DEPLOY-TEST1/**",
      "use": "@vercel/node",
      "config": {
        "functions": {
          "prebuild": "prisma generate"
        }
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/DEPLOY-TEST1/$1" }
  ]
}

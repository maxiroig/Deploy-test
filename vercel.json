{
	"version": 2,
    "installCommand": "pnpm install --no-frozen-lockfile",
	"builds": [
		{
			"src": "deploy-test1",
			"use": "@vercel/node",
			"config": {
				"functions": {
					"prebuild": "prisma generate"
				}
			}
		}
	]
}

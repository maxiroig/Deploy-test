{
	"version": 2,
	"builds": [
		{
			"src": "deploy-test1",
			"use": "@vercel/node",
			"config": {
				"functions": {
					"prebuild": "npx prisma generate &&  pnpm build",
                    "install": "--no-frozen-lockfile"
				}
			}
		}
	]
}

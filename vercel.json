{
  "version": 2,
  "builds": [
    {
      "src": ".nuxt",
      "use": "@vercel/node",
      "config": {
        "functions": {
          "prebuild": "prisma generate"
        }
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/nuxt/$1" }
  ]
}

{
	"version": 2,
	"builds": [
		{
			"src": "api/**",
            "use": "@nuxt/vercel",
			"config": {
				"functions": {
					"prebuild": "npx prisma generate &&  pnpm build"
				}
			}
		}
	],
    "installCommand": "pnpm install"
}

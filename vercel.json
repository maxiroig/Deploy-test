{
  "version": 2,
  "builds": [
    {
      "src": "/",
      "use": "@vercel/node",
      "config": {
        "functions": {
          "prebuild": "prisma generate"
        }
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)"}
  ]
}

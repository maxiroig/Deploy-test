{
	"version": 2,
    "installCommand": "pnpm install --no-frozen-lockfile",
	"builds": [
		{
			"src": "api/**",
            "use": "@nuxt/vercel",
			"config": {
				"functions": {
					"prebuild": "npx prisma generate &&  pnpm build"
				}
			}
		}
	]
}
